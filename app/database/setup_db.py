# app/database/setup_db.py
from app.database.db_handler import db

def initialize_database():
    """Initialize database tables if they don't exist"""
    # This is just a placeholder - we already have our SQL script
    print("Database setup complete (tables created via SQL script)")

if __name__ == "__main__":
    initialize_database()